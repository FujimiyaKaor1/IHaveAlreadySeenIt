# Security Notes

IHaveAlreadySeenIt 的目标不是让客户端修改变得“无风险”，而是让风险可见、可审计、可恢复。

## 保证范围

- 运行时不联网。
- 不收集或输出消息内容与账号信息。
- 安装前校验 Bundle ID、腾讯 Team ID、严格代码签名、版本、build、SHA-256、架构和唯一特征码。
- 所有外部命令均使用参数数组调用，不经过 shell 拼接。
- 拒绝符号链接、异常所有者和空间不足的目标；安装前完整备份并再次验证官方签名。
- 安装失败自动恢复；GUI 的高权限写入只通过签名、按需启动且接口受限的 Helper。
- 不申请 `get-task-allow`，不安装调试器；签名发行版的权限 Helper 仅按需启动，不常驻运行。

## 非保证范围

- 不能保证腾讯不会检测或限制被修改的客户端。
- 不能保证微信更新后仍兼容。
- 不能恢复安装前已经损坏或被第三方修改的应用。
- 不能保证撤回功能在服务端语义上的任何行为。

## 添加新版本规则

只有在以下证据齐全时才应添加新规则：官方签名原始应用、完整版本/build、主程序 SHA-256、arm64 与 x86_64 特征各唯一命中，以及在副本上的安装/卸载验证。不得仅依赖版本号或模糊特征。

## 报告漏洞

请使用 GitHub Security Advisory 私下报告可能导致任意命令执行、权限提升、错误修改 App、备份破坏或敏感数据暴露的问题。在修复发布前不要公开利用细节。

报告中不要包含微信二进制、聊天数据库、消息、账号标识、证书或私钥。可以附上 `doctor --json` 的输出和最小复现步骤。
