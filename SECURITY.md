# Security Notes

WeChatGuard 的目标不是让客户端修改变得“无风险”，而是让风险可见、可审计、可恢复。

## 保证范围

- 运行时不联网。
- 不收集或输出消息内容与账号信息。
- 安装前校验 Bundle ID、版本、build、SHA-256、架构和唯一特征码。
- 所有外部命令均使用参数数组调用，不经过 shell 拼接。
- 安装前完整备份原应用；安装失败自动恢复。
- 不申请 `get-task-allow`，不安装调试器或后台服务。

## 非保证范围

- 不能保证腾讯不会检测或限制被修改的客户端。
- 不能保证微信更新后仍兼容。
- 不能恢复安装前已经损坏或被第三方修改的应用。
- 不能保证撤回功能在服务端语义上的任何行为。

## 添加新版本规则

只有在以下证据齐全时才应添加新规则：官方签名原始应用、完整版本/build、主程序 SHA-256、arm64 与 x86_64 特征各唯一命中，以及在副本上的安装/卸载验证。不得仅依赖版本号或模糊特征。
